# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_dispatch --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: dispatch

on:
  workflow_dispatch:
    inputs:
      TweetBaseName:
        description: "Tweet Base Name"
        required: true

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Cache .nuke/temp, ~/.nuget/packages
        uses: actions/cache@v2
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj') }}
      - name: Run './build.cmd SendTweet'
        run: ./build.cmd SendTweet
        env:
          TweetBaseName: ${{ github.event.inputs.TweetBaseName }}
          GitHubToken: ${{ secrets.GITHUB_TOKEN }}
          TwitterConsumerKey: ${{ secrets.TWITTER_CONSUMER_KEY }}
          TwitterConsumerSecret: ${{ secrets.TWITTER_CONSUMER_SECRET }}
          TwitterAccessToken: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TwitterAccessTokenSecret: ${{ secrets.TWITTER_ACCESS_TOKEN_SECRET }}

# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_scheduled --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: scheduled

on:
  schedule:
    - cron: '0 13 * * 1,4'

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run './build.cmd Foo'
        run: ./build.cmd Foo
        env:
            GitHubToken: ${{ secrets.GITHUB_TOKEN }}
            TwitterConsumerKey: ${{ secrets.TwitterConsumerKey }}
            TwitterConsumerSecret: ${{ secrets.TwitterConsumerSecret }}
            TwitterAccessToken: ${{ secrets.TwitterAccessToken }}
            TwitterAccessTokenSecret: ${{ secrets.TwitterAccessTokenSecret }}
